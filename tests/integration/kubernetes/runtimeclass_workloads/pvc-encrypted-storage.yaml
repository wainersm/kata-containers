apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storage-encrypted
spec:
  accessModes:
  - ReadWriteOnce
  volumeMode: Block
  storageClassName: local-sc
  resources:
    requests:
      storage: 5Gi
